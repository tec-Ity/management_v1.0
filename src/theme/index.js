import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    custom: {
      primary: "#202753",
      primaryLight: "#2027531a",
      secondary: "#4C5FC7",
      secondaryMid: "#7C92E7",
      secondaryLight: "#C8D1EA",
      gray: "#E0E3F2",
      white: "#fff",
      whiteLight: "#ffffff80",
      selected: "#75FBE1",
    },
  },
});
